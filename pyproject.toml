[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "gwflow"
version = "0.1.0.dev0"
requires-python = ">=3.11"
description = "Versioned subpipeline planning"

[tool.setuptools.packages.find]
include = ["gwflow*", "examples*"]
